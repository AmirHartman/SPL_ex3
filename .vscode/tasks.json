{
    "tasks": [
        {
            "type": "shell",
            "label": "make",
            "command": "make",
            "args": [""],
            "options": {
                "cwd": "${workspaceFolder}/client"
            }
        },
        {
            "type": "shell",
            "label": "mvn compile",
            "command": "mvn",
            "args": ["compile"],
            "options": {
                "cwd": "${workspaceFolder}/server"
            }
        }
    ],
    "version": "2.0.0"
}